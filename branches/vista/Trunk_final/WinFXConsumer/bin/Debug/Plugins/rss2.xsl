<?xml version="1.0"?>
  <xsl:stylesheet 
      xmlns:xsl="http://www.w3.org/1999/XSL/Transform" version="1.0">
  <xsl:output method="html" version="1.0" 
      indent="yes" encoding="iso-8859-1"/>
  <xsl:template match="/">
    <html><body>
      <div style=
         "padding: 1em;background-color: #fafafa; border: 1px solid #cfcfcf;">

	<xsl:for-each select="rss/channel">
	  <div>
        <h2 style="color:blue; margin-left:50">
            <a>
                <xsl:attribute name="href">
                    <xsl:value-of select="link"/>
                </xsl:attribute>
                <xsl:value-of select="title"/>
            </a>
        </h2>
        <h4 style="color:blue; margin-left:50"><xsl:value-of select="description"/></h4>

        <!--    <h4><xsl:text> language: </xsl:text><xsl:value-of select="language"/></h4>
                                <h4><xsl:text> generated by:  </xsl:text><xsl:value-of select="generator"/></h4> -->
	  </div>
	</xsl:for-each>

        <xsl:for-each select="rss/channel/item">
          <xsl:variable name="stl">
            <xsl:text/>background-color: #efeff5; 
                border: 1px solid #cfcfcf;padding: 0em 1em 0em; margin:
            <xsl:text/>
            <xsl:choose>
              <xsl:when test="position()=last()"> 0em</xsl:when>
              <xsl:otherwise> 0em 0em 1em 0em</xsl:otherwise>
            </xsl:choose>
          </xsl:variable>
          <div>
            <xsl:attribute name="style"><xsl:value-of select="$stl"/>
            </xsl:attribute>
            <p><h3 style="color:maroon"><xsl:value-of select="title"/></h3>
            </p>
            <p><xsl:value-of disable-output-escaping="yes" 
                                               select="description"/>
            </p>
            <xsl:variable name="pub" select="pubDate"/>
            <xsl:if test="count($pub) > 0">
              <p align="right" 
                  style="margin:0; padding:0"><xsl:value-of select="pubDate"/>
              </p>
            </xsl:if>
 	  <i>         
	    <xsl:text> | </xsl:text>
     	  </i> 
	  <xsl:apply-templates select="category"/>

            <p style="margin:0; padding:0em 0em 1em 0em">
            <a target="_blank">
              <xsl:attribute name="href">
                <xsl:value-of select="link"/>
              </xsl:attribute>
              <xsl:value-of select="link"/>
            </a></p>
	    <p style="margin:0; padding:0em 0em 1em 0em"><a target="_blank">
              <xsl:attribute name="href">
                <xsl:value-of select="comments"/>
              </xsl:attribute>
              <xsl:value-of select="comments"/>
            </a></p>
          </div>
        </xsl:for-each>
      </div>
    </body></html>
  </xsl:template>

<xsl:template match="category"> 
     <i> 
          <xsl:value-of select="."/> 
	  <xsl:text> | </xsl:text>

     </i> 
</xsl:template>

</xsl:stylesheet>